module.exports = {
	"env": {
		// `airbnb-base` sets this to true. We reset it here,
		// to make the config extend logic clearer.
		"node": false
	},
	"extends": [
		"airbnb-base", // `eslint-config-airbnb-base` rules
	],
	"rules": {}
}